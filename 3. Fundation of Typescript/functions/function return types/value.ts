/**
 * Returns the error message as a string (used for returning, not logging).
 *
 * @param errmsg - The error message
 * @returns The same error message as a string
 */
function consoleError1(errmsg: string) {
    return errmsg;
}